<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_menu_gradient"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp"
        android:gravity="center_horizontal">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">
            <ImageView
                android:layout_width="72dp"
                android:layout_height="72dp"
                android:src="@drawable/ic_logo_emblem"
                android:contentDescription="@string/app_name" />
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:layout_marginStart="12dp">
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/app_name"
                    android:textColor="@color/brand_steel"
                    android:textSize="26sp"
                    android:textStyle="bold"
                    android:letterSpacing="0.05" />
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/tagline"
                    android:textColor="@color/brand_orange"
                    android:textSize="12sp" />
            </LinearLayout>
        </LinearLayout>

        <GridLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:columnCount="2"
            android:useDefaultMargins="true">

            <TextView style="@style/MenuLabel" android:text="@string/menu_country" />
            <Spinner android:id="@+id/spCountry" style="@style/MenuSpinner" />

            <TextView style="@style/MenuLabel" android:text="Driving Side" />
            <TextView android:id="@+id/tvDrivingSide" style="@style/MenuValue" android:text="—" />

            <TextView style="@style/MenuLabel" android:text="@string/menu_environment" />
            <Spinner android:id="@+id/spEnvironment" style="@style/MenuSpinner" />

            <TextView style="@style/MenuLabel" android:text="@string/menu_vehicle" />
            <Spinner android:id="@+id/spVehicle" style="@style/MenuSpinner" />

            <TextView style="@style/MenuLabel" android:text="@string/menu_weather" />
            <Spinner android:id="@+id/spWeather" style="@style/MenuSpinner" />

            <TextView style="@style/MenuLabel" android:text="@string/menu_time" />
            <Spinner android:id="@+id/spTime" style="@style/MenuSpinner" />

            <TextView style="@style/MenuLabel" android:text="@string/menu_mode" />
            <Spinner android:id="@+id/spMode" style="@style/MenuSpinner" />
        </GridLayout>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/menu_adas"
            android:textColor="@color/brand_blue"
            android:textSize="16sp"
            android:textStyle="bold"
            android:layout_marginTop="14dp" />

        <!-- ADAS toggles are added programmatically into this container. -->
        <LinearLayout
            android:id="@+id/adasToggles"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />

        <Button
            android:id="@+id/btnStart"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginTop="18dp"
            android:text="@string/menu_start"
            android:textColor="@color/white"
            android:textSize="20sp"
            android:textStyle="bold"
            android:background="@drawable/btn_primary" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/disclaimer"
            android:textColor="@color/brand_steel"
            android:textSize="11sp"
            android:alpha="0.6"
            android:gravity="center"
            android:layout_marginTop="14dp"
            android:paddingBottom="10dp" />
    </LinearLayout>
</ScrollView>
